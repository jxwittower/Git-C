name: HSK Public Probe
on:
  push:
    branches:
      - hsk-crawl-20260914
    paths:
      - 'hsk_flutter_probe.js'
      - 'hsk_bundle_inspect.js'
      - 'package.json'
      - '.github/workflows/hsk-public-probe.yml'
  workflow_dispatch:
permissions:
  contents: read
jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v4
        with:
          ref: hsk-crawl-20260914
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: npm install --no-audit --no-fund
      - name: Inspect HSK Flutter bundle
        run: node hsk_bundle_inspect.js
      - name: Upload bundle report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: hsk-bundle-report
          path: hsk_bundle_report
          if-no-files-found: warn
